1. 第一部分 存储引擎

``` java
private int getValidNumForMaterialBrandId(List<Map<String, Object>> supplierInventory) {
    Optional<Map<String, Object>> firstInventory = supplierInventory.stream().findFirst();
    return firstInventory.map(map -> (int) map.get("validNum")).orElse(0);
}

...

itemlist.forEach(data -> {
    String f_material_brand_id = String.valueOf(data.get("f_material_brand_id"));
    List<Map<String, Object>> supplierInventory = groupedInventoryBySupplierId.getOrDefault(f_material_brand_id, new ArrayList<>());
    int validNum = getValidNumForMaterialBrandId(supplierInventory);
    data.put("validNum", validNum);
});

```